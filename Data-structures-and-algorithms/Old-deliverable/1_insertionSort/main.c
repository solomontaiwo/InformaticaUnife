#include "insertionSort.h"

int main(int argc, char *argv[])
{
    int min_length = 1000;
    int max_length = 50000;
    experiment(min_length, max_length); // chiamata della funzione "experiment" con parametri di minima e massima lunghezza degli array

    return 0;
}