#include <stdio.h>

char istruzioniValori() { return printf("%s", "Inserisci due numeri interi dal valore uguale o superiore a 0:\n"); }

char istruzioniOperatori() { return printf("%s", "Dimmi che tipo di operazione eseguire (1 = addizione, 2 = sottrazione, 3 = moltiplicazione, 4 = divisione):\n"); }
